import styles from './ContactList.module.css';

export default function ContactList() {
  return (
    <ul className={styles.list}>
      {/* TODO: Render filtered contacts here. */}
    </ul>
  );
}
