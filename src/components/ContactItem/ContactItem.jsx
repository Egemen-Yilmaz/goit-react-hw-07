import styles from './ContactItem.module.css';

export default function ContactItem() {
  return (
    <li className={styles.item}>
      {/* TODO: Render a single contact row with delete action. */}
    </li>
  );
}
